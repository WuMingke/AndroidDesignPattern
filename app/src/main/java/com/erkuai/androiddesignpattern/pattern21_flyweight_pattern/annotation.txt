享元对象中的部分状态是可以共享的，可以共享的状态成为内部状态，内部状态不会随着环境变化；不可共享的状态则称之为外部状态，
它们随着环境的改变而改变。在享元模式中会建立一个容器对象，在经典的享元模式中该容器为Map，它的键是享元对象的内部状态，
它的值是享元对象本身。客户端程序通过这个内部状态从享元工厂中获取享元对象，如果有缓存则使用缓存对象，否则创建一个享元对象
并且存入容器中，这样一来就避免了创建过多对象的问题。